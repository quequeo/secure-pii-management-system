name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  docker-compose:
    name: Docker Compose Build
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build Docker images
      run: docker compose build

    - name: Verify docker-compose configuration
      run: docker compose config

  security-check:
    name: Security Checks
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Check for secrets in code
      run: |
        echo "🔍 Checking for potential secrets..."
        # Check for hardcoded passwords (not ENV vars or placeholders)
        if grep -rn "password.*=.*['\"][^$<].*['\"]" --include="*.rb" rails-app/ | grep -v "ENV\|fetch\|placeholder\|example"; then
          echo "⚠️  Warning: Found potential hardcoded passwords"
          exit 1
        fi
        echo "✅ No hardcoded secrets found"

    - name: Verify .gitignore patterns
      run: |
        echo "🔍 Checking .gitignore configuration..."
        
        # Check that .env is ignored (in root or rails-app)
        if ! grep -q "^\.env$" .gitignore 2>/dev/null && ! grep -q "^\.env$" rails-app/.gitignore 2>/dev/null; then
          echo "❌ .env files not properly ignored"
          exit 1
        fi
        
        # Check that coverage is ignored in rails-app
        if ! grep -q "coverage" rails-app/.gitignore 2>/dev/null; then
          echo "❌ coverage directory not ignored in rails-app"
          exit 1
        fi
        
        echo "✅ .gitignore properly configured"

