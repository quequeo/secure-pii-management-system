# Application Configuration
spring.application.name=ssn-validation-service
server.port=8080

# Rate Limiting Configuration
rate.limit.requests-per-minute=100

# Logging
logging.level.com.pii.validation=INFO

